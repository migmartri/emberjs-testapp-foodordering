{{#if currentCompany.isSignedIn}}
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <a class="navbar-brand" href="#">{{currentCompany.name}}</a>
    <ul class="nav navbar-nav">
      <li class="active">
        {{#link-to 'index'}}Current order{{/link-to}}
      </li>
      <li>
        {{#link-to 'products'}}Products{{/link-to}}
      </li>
    </ul>
    <ul class='nav navbar-nav pull-right'>
      <li class='dropdown'>
        <a href='#' class='dropdown-toggle navbar-link' data-toggle="dropdown">
          {{currentCompany.name}}
          <i class='caret'></i>
          </a>
            <ul class='dropdown-menu'>
              <li> <a href='#' {{action 'logout'}}>Logout</a></li>
           </ul>
      </li>
     </ul>
  </nav>
{{/if}}
<div id='container' class='container'>
  {{outlet}}
</div>
